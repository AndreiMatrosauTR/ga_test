name: Java CI with Maven

on:
  push:
    branches: [ integration ]
  pull_request:
    branches: [ integration ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout the repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'zulu'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml
      # env:
      #     TR1_ACCOUNT_NAME: ${{ secrets.TR1_USER }}
      #     TR1_API_TOKEN: ${{ secrets.TR1_TOKEN }}
    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v3.1.0
      if: success() || failure()
        with:
          # Artifact name
          name: test-results
          # A file, directory or wildcard pattern that describes what to upload
          path: jest-junit.xml

    #
    #   - name: Build with maven
    #     run: mvn --batch-mode --update-snapshots --file=pom.xml -DskipTests package
    #
    #   - name: Test
    #     run: mvn --batch-mode -Dmaven.test.failure.ignore=true test
    #

      # - name: Upload
      #   uses: actions/upload-artifact@v3
      #   if: success() || failure()
      #   with:
      #     name: test-results
      #     path: jest-junit.xml
      #
      # - name: Test report
      #   uses: dorny/test-reporter@v1
      #   with:
      #     artifact: test-results
      #     name: JEST Tests
      #     path: '*.xml'
      #     reporter: jest-junit
